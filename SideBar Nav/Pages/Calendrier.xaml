<Page x:Class="TheClassMain.Pages.Calendrier"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
 xmlns:local="clr-namespace:TheClassMain.Pages"
 mc:Ignorable="d" 
 d:DesignHeight="600" d:DesignWidth="1000"
 Title="Calendrier">

    <Grid Margin="20"  Background="AliceBlue">


        <TextBlock Text="Calendrier 26xx"

 FontSize="70"
 FontWeight="Bold"
 Foreground="#FF7A5CFA"
 HorizontalAlignment="Center"
 />

        <Border  Margin="20,100,0,20" Width="380"  HorizontalAlignment="Left" >
            <Calendar Name="CalendrierCool26x"

 FontSize="18"
 Background="White"/>
        </Border>

        <Border  Background="White" Margin="420,100,20,20" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Name="rapelleDujour"
 Text="Choisis une date ! "
 FontSize="24"
 FontWeight="Bold"/>

                <ListView ItemsSource="{Binding FacturesDuJour}" Margin="20" Height="200">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Numéro" DisplayMemberBinding="{Binding UserFactureId}" />
                            <GridViewColumn Header="Description" DisplayMemberBinding="{Binding Description}" />
                            <GridViewColumn Header="Montant" DisplayMemberBinding="{Binding Montant, StringFormat={}{0:C}}" />
                            <GridViewColumn Header="Catégorie" DisplayMemberBinding="{Binding Categorie.Name}" />
                            <GridViewColumn Header="Date" DisplayMemberBinding="{Binding Date, StringFormat='{}{0:dd/MM/yyyy}'}" />
                        </GridView>
                    </ListView.View>
                </ListView>
            </StackPanel>
        </Border>
    </Grid>
</Page>